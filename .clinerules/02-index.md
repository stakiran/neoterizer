# 目次ファイル
docs/ 配下のすべての Markdown ファイルのリンク集をつくりたい。これを目次ファイルという

目次ファイルは index.md として保存する。

目次ファイルの中身は次のとおり。

```
# 目次
- [ファイル名](ファイル名.md)
- [ファイル名](ファイル名.md)
……
```

並べる順番はファイル名は昇順で構いません。

# 実装
- Python で実装します
- 実行は `python index.py` で行います
